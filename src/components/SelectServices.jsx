export const SelectServices = ({ onNext }) => {
    // Some logic or content for selecting services
    return (
      <div>
        {/* ...your select services content */}
        <button onClick={onNext}>Next</button>
      </div>
    );
  };